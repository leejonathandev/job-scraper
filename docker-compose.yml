version: '3.8'

services:
  job-scraper:
    image: ghcr.io/leejonathandev/job-scraper:latest
    cap_add:
      - SYS_ADMIN
    environment:
      - NODE_ENV=production
      - NOTIFICATION_WEBHOOK=${NOTIFICATION_WEBHOOK}
    restart: unless-stopped
